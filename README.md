# general-knowledge-quiz-cli
An interactive CLI quiz application engineered in Python using the IPOS architecture, featuring input sanitization pipelines (.strip().lower()) and dynamic f-string formatting.
